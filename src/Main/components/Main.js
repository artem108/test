import React, { Component } from 'react'
import styled from 'styled-components'
import { RevealPRight }  from './../../styled/Home.style'

class Main extends Component {

  render() {

    const {
      text,
      title,
      styleTextHome
    } = this.props

    return (
      <section>
  
    </section>
    );
  }
}

export default Main
